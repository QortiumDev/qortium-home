name: CodeQL

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "29 8 * * 1"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  packages: read
  security-events: write

jobs:
  analyze-javascript-typescript:
    name: Analyze JavaScript/TypeScript
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4.37.8
        with:
          languages: javascript-typescript

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v4.37.8
        with:
          category: "/language:javascript-typescript"

  analyze-java-kotlin:
    name: Analyze Java/Kotlin
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: Install npm dependencies
        run: npm ci

      - name: Install Android SDK packages
        run: |
          sdkmanager_bin="${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager"
          if [ ! -x "$sdkmanager_bin" ]; then
            sdkmanager_bin="$(command -v sdkmanager)"
          fi

          yes | "$sdkmanager_bin" --licenses >/dev/null
          "$sdkmanager_bin" "platforms;android-36" "build-tools;35.0.0"

      - name: Sync Capacitor Android project
        run: npm run android:sync

      - name: Prepare Android debug build for Java analysis
        working-directory: android
        run: ./gradlew --no-daemon :app:assembleDebug

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4.37.8
        with:
          languages: java-kotlin
          build-mode: manual

      - name: Compile Home Android Java sources
        working-directory: android
        run: |
          ./gradlew --no-daemon \
            :app:compileDebugJavaWithJavac \
            --rerun-tasks \
            -x :capacitor-android:compileDebugJavaWithJavac \
            -x :capacitor-app:compileDebugJavaWithJavac \
            -x :capacitor-filesystem:compileDebugKotlin \
            -x :capacitor-filesystem:compileDebugJavaWithJavac \
            -x :capacitor-preferences:compileDebugJavaWithJavac \
            -x :capacitor-cordova-android-plugins:compileDebugJavaWithJavac

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v4.37.8
        with:
          category: "/language:java-kotlin"
